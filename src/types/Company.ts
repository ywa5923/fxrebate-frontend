import { OptionValue } from "./OptionValue";

export type DynamicTable = {
    id: number;
    option_values: OptionValue[]|null;
}

export type Company = {
    id: number;
    option_values: OptionValue[]|null;
}